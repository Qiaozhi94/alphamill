# F008 执行机取证：容量预检（T002）与真实发现/冻结（T019）

- 取证机器：`qiaozhi-lt`（Win11 + WSL2，RTX 4060 Laptop）—— 本仓执行机，架构 §7.1
- 取证时间：2026-09-21（UTC+8 晚）
- 取证方式：本机直接执行（非开发机代跑），命令与输出见下

## T002 磁盘余量预检（`NFR-005`）

`df -h`（执行机）：

```text
Filesystem      Size  Used Avail Use% Mounted on
/dev/sdd       1007G   90G  866G  10% /            # / 与 /var/lib/docker 同盘
```

6 对基线实测（F002 时代数据，本次实测）：

| 项 | 实测值 | 来源 |
|---|---|---|
| 湖总占用 | 259 MB（ohlcv_1m 212 MB） | `du -sh lake/` |
| 湖分区文件数 | 8,858 | `find lake -name '*.parquet' \| wc -l` |
| DB 卷占用 | 1.543 GB（`deployment_timescale_data`） | `docker system df -v` |
| 库内行数 | 6,358,620（`ohlcv_1m`） | `SELECT count(*)` |

40 对外推（×6.67）：

| 项 | 外推值 | 判据 |
|---|---|---|
| 湖占用 | ≈1.7 GB | 259 MB × 40/6 |
| 分区文件数 | ≈59,000 | 8,858 × 40/6（`NFR-005` 估 58,800 ✓） |
| DB 卷 | ≈10.3 GB | 1.543 GB × 40/6 |
| 合计新增 | **≈12 GB** | 远小于可用 866 GB |

**结论：余量充足，无需扩容**；回填启动期另有 `capacity.require_headroom`（1.2 倍余量）代码闸门，
本批 30 对估算 8.3 GB，实测通过。

## T019 真实发现与冻结（`FR-001` / `FR-002`）

出口：执行机不在家网，`deployment/.env` 里原配的 `BINANCE_HTTPS_PROXY=http://10.31.0.254:7890`
（家里 NAS 的 LAN 地址）不可达 → 生产采集也因此在 2026-09-19 14:03 起连续 0 行入库。
owner 授权后改为 Tailscale 可达的 `http://100.78.41.62:7890`（NAS mihomo），并
`docker compose up -d --force-recreate data-collector` 重建容器（`restart` 不重载 env），
采集恢复：`symbols_ok=6 symbols_failed=0 rows_upserted=24`。

发现（`python -m alphamill.data_bridge.universe discover`）：

- 快照时间：`2026-09-21T14:33:54Z`；市场数 726；选中 40；排除 686
- 排除分布：`tokenized_tradfi` 200、`no_spot_market` 145、`rank_below_top_n` 312、
  `listed_days_not_enough` 27、`leveraged_token` 1、`stablecoin_pair` 1
- **universe_id**：`sha256:b40651e2a101c6ba3253d01e2e811d1e0d6db1008505637c6dbabc2f53de9804`
- 定义：`lake/_metadata/universe_defs/sha256:b40651e2….json`
- 冻结：`2026-09-21T14:36:46Z`，`frozen_by=georg`，`selected=40`
- 现有 6 对全部在位；`1000PEPE→PEPE`、`1000SHIB→SHIB` 按同标的映射到现货命名空间

首轮发现（同口径未加数据可达性前置）暴露的问题与 owner 裁决：40 对里 12 对在 Binance
现货不存在（回填路线取不到数）：`1000PEPE`（命名口径）、`HYPE/AKE/LAB/VELVET/BEAT`
（只有永续）、`XAU/XAG/MSTR/CRCL/EWY/INTC`（代币化商品/股票，`TRADIFI_PERPETUAL`）。
裁决：口径加**数据可达性前置**与**代币化 TradFi 排除**（按 Binance `underlyingType`/`contractType`
判别，非硬编码名单），重新发现后如上。

## 批 1 回填（T020，进行中）

- 启动器：`scripts/f008-backfill.sh 1`（nohup 常驻；中断后原样重跑即从断点续跑）
- run_id：`b40651e2a101-b1-20260921T152932Z`（首次尝试 `…T151926Z` 因 4 个 pair 遭遇代理链路
  瞬时重置判 failed 而重启；已修 `is_retryable_error` 把瞬时网络故障纳入退避重试）
- 运行记录：`reports/backfill/<run_id>/run.json`；日志：`reports/backfill/batch1-*.log`
- 6 对现有 pair 由进度账本识别为 `already_complete` 直接跳过（rows 取库内实测 1,052,152/对；
  `ledger_rows` 保留账本原值——生产库里 BNB/ETH/SOL 的账本计数为 0，实测才发现）

后续（不在本次会话内完成）：批 1 完成后跑 `gate`（先刷新连续聚合）→ 准入；再启批 2；
两批过门后跑全量导出与 NAS 备份实测（T023/T026）；最后 T034 回写验收证据与状态。
