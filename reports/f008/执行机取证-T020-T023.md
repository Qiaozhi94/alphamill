# F008 执行机取证：批 1/批 2 回填与准入切换（T020–T023）

- 取证机器：`qiaozhi-lt`（Win11 + WSL2，RTX 4060 Laptop）—— 本仓执行机，架构 §7.1
- 取证时间：2026-09-22 ~ 2026-09-23（UTC；本机 `hostname=qiaozhi-lt`）
- 取证方式：本机直接执行（非开发机代跑），命令与实测输出见下
- 关联运行记录：`reports/backfill/<run_id>/run.json`（入库）；逐片日志 `reports/backfill/chunk-b1-*.log{,.err}`（gitignore）

## 1. 分片执行方式（T020/T021）

数天的长跑改为**时间片循环**：`scripts/f008-backfill-chunks.sh <batch>` 调
`universe backfill --max-runtime-minutes 30`，到点把当前断点写成 `running` 并抛
`BackfillDeadlineReached`——**`deferred` 不是失败**，下一片从 `backfill_progress.next_since` 续跑。
所有分片共用同一份 `BackfillRun`（`--resume-run-id`），因此逐 pair 证据不碎。

批 1 的运行记录：`run_id=6d85a249e8fe-b1-20260922T150054Z`（`universe_id=sha256:6d85a249e8fe…`）。

## 2. 实测速率（本机，2026-09-23）

逐组件实测（`--fetch-limit 1000`）：

| 组件 | 实测耗时 | 说明 |
|---|---|---|
| `upsert_ohlcv` 1000 行 | **72 ms**（中位，5 次） | `execute_values` 批量 upsert + commit，含并发回填负载 |
| `save_progress` | **4 ms** | 断点写入 |
| ccxt `fetch_ohlcv(limit=1000)` | **291 ms**（中位；首次 3.2 s 为 TLS 建连） | 经 NAS mihomo（Tailscale）代理 |
| 理论单批 | ≈0.9 s | 限速器 `min_interval_seconds=0.5` 为主项 |

分片实测：

| 片 | 时间窗（UTC） | 行数 | 速率 | 说明 |
|---|---|---|---|---|
| 1 | 09-22 15:00:53→15:31:14 | ≈1.05 M | ≈35 k 行/分 | ZEC/NEAR/BANK 起步 + 6 对 `already_complete` |
| 2 | 09-22 15:43:54→16:13:56 | 306 k | **≈10.2 k 行/分** | 代理链路拥塞（异常值，见下） |
| 3 | 09-23 04:52:02→05:22:03 | **3.06 M** | **≈102 k 行/分** | 0 次重试；AAVE/ACE/ADA 完成 |
| 4 | 09-23 06:08:46→06:43:31 | 1.03 M | ≈34 k 行/分 | **出口代理事故**（见 §5）：ARB/AVAX 判 failed，5 分钟超时 |
| 5 | 09-23 07:24:56→07:55:03 | 260 k | ≈8.7 k 行/分 | **tailnet 退化为 DERP 中继**（见 §6） |
| 6 | 09-23 09:35:35→10:05:36 | **3.5 M** | **≈118 k 行/分** | tailnet 恢复直连后满速；ARB/AVAX/BANK/BCH/DEXE 完成 |

片 2 的低速为**瞬时链路抖动**而非配置问题：同机复测组件耗时之和 ≈0.9 s/批，片 3 实测
≈0.5 s/批（含限速 0.5 s），故未调整任何限速参数。**限速侧余量充足**：Binance 现货
`klines limit=1000` 单请求权重 **4**（读响应头 `x-mbx-used-weight` 46→50 实测），
1 分钟用量约 **50/6000**（≈8%）；实测 `min_interval=0.5s` ⇒ ≈8 权重/秒。速率瓶颈不在配额。

数据完整性抽查：`ZEC/USDT` 88,330 行 = 88,330 个不同时间戳（无重复）。

## 3. 批 1 台账状态（截至 2026-09-23 10:05Z，片 6 结束）

`backfill_progress`（窗口 `2024-09-10T00:00Z → 2026-09-10T15:52Z`）：

- **14/30 complete**：F001 基线 6 对（BTC/ETH/SOL/BNB/XRP/DOGE）+ AAVE、ACE、ADA、ARB、
  AVAX、BANK、BCH、DEXE
- 16 running，其中已推进：ZEC 658,870、ENA 413,000、NEAR 34,300
- 批 1 剩余 **≈14.8 M 行**、批 2 **≈10.0 M 行**；按 118 k 行/分 ⇒ 批 1 ≈2.1 h（再 4–5 片）、
  批 2 ≈1.4 h（3 片）

剩余量的口径：逐 pair 按 `max(listed_at, window_start)` 到 `window_end` 的**实际可得分钟数**
计（上线晚于窗口起点的 5 对：BANK、TRUMP、PUMP、TUT、DEXE 不足 1,052,152 行），
减去已推进行数。

## 4. 准入切换的交付缺口与 owner 决策（T023/T034 前置）

**发现（本会话核对生产配置）**：`deployment/.env` 的 `SYMBOLS` 是**静态 6 对**，
而 `collector/ccxt_ingestor.py` 每轮只取最近 `FETCH_LIMIT=5` 根 K 线，**没有按最后时间戳追赶**
的逻辑；F008 三件套亦未覆盖「准入时更新采集清单」这一步。

后果：若不处理，准入后 34 个新 pair 的数据只到冻结窗口终点 `2026-09-10T15:52Z`，
实时数据要等清单更新+容器重建才开始——截至 2026-09-23 该洞已 13 天且每日增长，
会让扩容后的宇宙在研究上直接断档。

**owner 决策（2026-09-23，选项 A）**：在 T023/T034 收口时执行两步，保证序列连续：

1. 把 `deployment/.env` 的 `SYMBOLS` 扩到**已准入的 40 对**，并
   `docker compose up -d --force-recreate data-collector`——**必须 recreate**：
   `restart` 不重载 env（2026-09-21 代理修复时已实测踩到）。
2. 切换前跑**追赶回填**：`--start 2026-09-10T15:52:00Z --end <切换时刻>`，
   34 对 × 13 天 ≈ 64 万行 ≈ **6 分钟**（按 102 k 行/分）；`gate` 支持 `--start/--end`，
   如需对延长窗口重新判定可原样重跑门禁。

后续候选（不在 F008 范围，收口时再决定是否入 `BACKLOG.md`）：把采集清单从静态 env
改为由 `universe_membership` 台账驱动，让「准入即采集」成为代码保证而非人工步骤。

## 5. 事故：NAS mihomo 出口节点挂掉（2026-09-23 06:08–06:18、06:38–06:43）

现象：代理 TCP 端口可连、NAS 可 ping 通，但 HTTPS CONNECT 全部挂起 → 分片里每个 pair
各自耗尽 8 次退避重试（约 6 分钟）才判 `failed`，ARB 因此在片 4 留下假失败。

诊断：NAS mihomo 日志 `[TCP] dial 黑名单网站 ... error: net/http: cannot rewind body after
connection loss` + `because 自动选择 failed multiple times, activate health check`；
查控制面 `/proxies` 得链路 `黑名单网站 → 主代理 → 自动选择 → SG-2`，而 SG-2 已死且
URLTest 组没有切走。逐个探活：SG-1 80 ms、SG4-HY2 74 ms、JP-1 114 ms 存活；
SG-2/SG-3/SG5-HY2/US-1TCP/HK-1/HK-2 全部 Timeout。

处置：`PUT /proxies/自动选择 {"name":"SG-1"}` 切到存活节点，出口立即恢复
（Binance 200 / 0.25 s），片 4 自动续跑。**属瞬时故障**：09:35 复测 49 个节点全部健康
（0 Timeout，SG-1 72 ms / SG-2 78 ms），无需刷新订阅（`config.yaml` 仍为 2026-09-15 版本）。

防复发（已入库）：`scripts/f008-backfill-chunks.sh` 增加**开片前置检查**——只读探测
Binance 可达性（连试 3 次避免冷启动假失败），不通则 `exit 2` 且**本片不启动**，
不再让每个 pair 烧完退避才判 failed。

## 6. 事故：lt 的 tailnet 退化为 DERP 中继（2026-09-23 07:55–09:35）

现象：片 5 速率从 102 k 掉到 **8.7 k 行/分**；`tailscale ping` 显示
`via DERP(hkg)`、`direct connection not established`，RTT 0.9–2.6 s。

关键区分（避免误判为节点问题）：

- **lt 本地互联网正常**：AliDNS 0% 丢包 / 28 ms；
- **NAS 侧代理正常**：从 NAS 直接取 1000 根 K 线 0.26–0.57 s；
- **瓶颈是中继带宽 ≈15 KB/s**：采集器每轮只取 5 根（~1 KB）故 3–5 s 完成、0 失败、
  延迟 1 分钟——**生产采集未被拖垮，只有大响应（1000 根 ≈40 KB gzip 后）被卡住**；
- gzip 已在生效（ccxt 默认发 `Accept-Encoding: gzip, deflate`，见 `ccxt/base/exchange.py:517`），
  压缩后 40 KB vs 未压缩 158 KB，故此路无进一步优化空间。

处置：owner 以管理员重启 lt 的 Tailscale 服务后恢复**直连**——
`direct [2408:823c:a616:d27d::2a0]:41641`、ping 63 ms、1000 根 K 线 0.62–0.82 s；
片 6 随即回到 **118 k 行/分**。`netcheck` 记录：`MappingVariesByDestIP: true`（对称 NAT，
打洞困难）、无 PortMapping——若再次退化，重启 Tailscale 是首选处置。

## 7. 发现：4 对的 `listed_at` 是永续上市日，门禁判的却是现货数据（T022 前置）

`exchange_snapshot.py:147` 从**永续**市场取 `onboardDate` 作为 `listed_at`（口径按永续成交额
排名），该日期被写入定义，随后被 `quality_gate.effective_window()` 当作**现货**数据的窗口起点、
被 `admission.admit_pair()` 当作台账 `valid_from`。用 Binance 原始接口逐对核对（窗口起点或
`listed_at` 起取首根 K 线），4 对存在实质差异：

| pair | 定义 `listed_at`（永续） | 现货数据实际起点 | 差 |
|---|---|---|---|
| BANK | 2025-04-18 | 2025-11-13 | 208.8 天 |
| ONDO | 2024-01-20 | 2025-04-11 | 213.6 天 |
| PUMP | 2025-07-10 | 2025-09-11 | 63.2 天 |
| TUT | 2025-03-20 | 2025-03-27 | 7.5 天 |
| TRUMP | 2025-01-18 | 2025-01-19 | 0.8 天（可忽略） |

已确认**不是回填漏抓**：`klines?symbol=BANKUSDT&startTime=2025-04-18` 直接返回 2025-11-13 起，
日线同样从 2025-11-13 开始——源头就没有那段数据。

后果：T022 一跑，这 4 对按上述窗口算缺失率约 41% / 29% / 15% / 1.4% → `QUARANTINED`，
扩容后宇宙由 40 对降到 36 对；台账 `universe_at(T)` 还会在「永续上市 → 现货有数据」的空窗里
声称它们可交易。这与 `AC-006` / `DQ-004`「按**实际可得窗口**算缺失率，不误判」的意图不符，
属实现与文档意图的偏离。

**owner 决策（2026-09-23，选项 A）**：本轮**不改门禁语义**，让 T022 按现语义跑并把 4 对的
隔离结果作为证据；「门禁与台账改用**数据命名空间的上市时间**」（发现阶段记录现货首根 K 线
时间，门禁窗口与 `valid_from` 都用它）作为**独立修复项单独评审**——它同时改变 point-in-time
台账的 `valid_from` 语义，需要重新冻结宇宙（新 digest），不宜在回填途中顺手改。T034 收口时
须回看此项是否已立项。

## 8. 提速实测：限速间隔 0.5s → 0.2s（`DQ-002`「实测后再调」）

背景：批 1 实测吞吐稳定在 **118 k 行/分 ≈ 118 请求/分**，与 `min_interval=0.5s` 算出的
120 请求/分吻合，怀疑限速器而非配额是瓶颈。逐项实测（2026-09-23，执行机）：

| 测量 | 结果 |
|---|---|
| 单请求权重 | `klines limit=1000` = **4**（响应头 `x-mbx-used-weight` 增量），配额 6000/分钟/IP |
| 冷连接取 1000 根（每次新建 TLS） | 约 0.6–0.9 s |
| **热连接取 1000 根**（`requests.Session` + gzip，与 ccxt 同形态） | **中位 0.226 s**（0.217–0.294，n=10） |
| 落库 1000 行 + 断点写入 | 72 ms + 4 ms |
| **单批串行地板** | **≈0.31 s**（请求 0.226 + 落库 0.08） |

结论：0.5 s 的间隔高于 0.31 s 的地板，**确实在卡**；降到 0.2 s 后间隔不再成为约束，
预期 **≈193 k 行/分（约 1.6×）**——不是线性放大，因为已触及串行地板。再往下调无益，
要更快只能上 pair 级并发（`DQ-002` 允许「pair 间可有限并发」，尚未实现）。

权重侧仍然宽松：0.2 s ⇒ 1200 权重/分 = 配额 20%（另加采集器约 12 权重/分、重试余量）。
处置：`scripts/f008-backfill-chunks.sh` 默认 `MIN_INTERVAL=0.2`（可用 `MIN_INTERVAL=0.5`
一键回退保守值）；库内 `RateLimitPolicy` 默认保持 0.5 不变，`--min-interval` 属运营参数，
逐片 `*.log.json` 与 `run.json` 都会记录实际取值，证据可追溯。

## 9. 批 1 质量门与准入（T022，2026-09-24）

命令：`python -m alphamill.data_bridge.universe gate --universe sha256:6d85a249e8fe… --start 2024-09-10T00:00:00Z --end 2026-09-10T15:52:00Z --pairs <批 1 的 30 对>`
（`gate` 无 `--batch`，按批次执行须显式给 `--pairs`；先刷新连续聚合，再逐对判四项并准入）

**结果：25 对 `ACTIVE`（已准入）、5 对 `QUARANTINED`**：

| pair | 判定 | 原因码 | 说明 |
|---|---|---|---|
| BANK | QUARANTINED | `missing_ratio_exceeded` | 现货晚于永续 208.8 天（§7 已裁决） |
| ONDO | QUARANTINED | `missing_ratio_exceeded` | 213.6 天 |
| PUMP | QUARANTINED | `missing_ratio_exceeded` | 63.2 天 |
| TUT | QUARANTINED | `missing_ratio_exceeded` | 7.5 天 |
| TRUMP | QUARANTINED | `boundary_not_closed` | 0.8 天（同类，跨窗口起点边界未闭合） |

准入产物：`universe_membership` **25 spot + 25 perp = 50 行**；判定记录最新态
25 ACTIVE / 5 QUARANTINED；最终 canonical artifact
`sha256:1795b0d206e0499c38610e9b31354dd7d1d7587f3e0ad96b03692c5cfb5e35d5.json`
（50 条成员，`IR-002` 三键，含 DEXE）。

### 过程中修掉的三个 F008 缺陷（均属门禁自身，非数据问题）

1. **聚合刷新不物化「跨窗口终点的不完整桶」**（`fb3ab20`）：首跑 30 对**全**判
   `aggregate_mismatch`，差值在 5m/15m/1h/4h/1d **所有桶宽上恒为 24**——24 个新 pair
   各缺窗口最后一天（2026-09-10）的日桶，其数据止于窗口终点 15:52，而
   `refresh_continuous_aggregate(view, start, end)` 不物化跨越 `end` 的桶。实测
   AAVE 该桶刷新前 0 行、末端外扩到 09-12 后 1 行。修法：刷新末端再外扩一个桶宽。
2. **对账两侧窗口起点口径不一致**（`4d1ba2f`）：DEXE 的 `listed_at=2024-12-24T11:30`
   不落在小时桶边界，基表侧按「桶与窗口重叠」计入 11:00 那个不完整桶、聚合侧按
   `bucket >= 11:30` 排除它 → 1h/4h/1d 各差 **25**（恰等于 11:30–12:00 有数据的
   symbol 数，实测确认），而 5m/15m 因 11:30 对齐而全等。修法：先用 `time_bucket()`
   把起点向下对齐，两侧使用同一对齐值，并把 `aligned_start` 入档。
3. **门禁复跑不幂等**（`ef89127`）：`append_membership` 的幂等契约在调用方
   （`(lake_pair, valid_from)` 去重），而 `admit_pair` 未去重 → 复跑在 BTC 上被只追加
   触发器拒（「追加序非法」）而整轮中断。修法：追加前与台账对账，只写缺失的行。

### 另一处部署缺口（收口时须同步）

`db/migrations/005_universe_membership.sql` 此前**从未在生产库执行**（`schema_migrations`
只有 003/004），首次 `gate` 直接撞 `relation "universe_quality_verdicts" does not exist`；
经 owner 同意后用 `tools/apply_migrations.py` 应用（纯新增、幂等）。**部署检出
`/home/georg/projects/alphamill` 里没有该文件**，F008 并入 `main` 并部署后才会带上——
T034 收口须核对。

### 对 F003 前提的影响（须回看 tasks.md）

`tasks.md` 原写「批 1 过门即满足 `F003` T035 的 ≥30 对前提」，但批 1 实际过门 **25 对**
（5 对被隔离），故该前提**需等批 2**（25 + 批 2 至多 10 = 至多 35）。

## 10. 批 2 回填与门禁（T021 / T022 第二次执行，2026-09-24）

批 2 = 第 31–40 名：LTC、SYN、ONG、COTI、DOT、DASH、SHIB、PENGU、PROM、HEI。

- 回填：**10/10 `complete`**（`run_id=6d85a249e8fe-b2-20260924T081842Z`）
- 门禁：**10/10 `ACTIVE`**（`gate-b2-20260924T120904Z.json`，无隔离）
- 门禁前逐对核对「定义 `listed_at` vs 现货首根 K 线」：**10 对全部差 0 天**（含上线晚于
  窗口起点的 PENGU/PROM/HEI 也精确吻合），这正是批 2 全过的原因
- 宇宙累计：**35 对 ACTIVE + 5 对 QUARANTINED**（批 1 的 25/5 + 批 2 的 10/0）；
  `universe_membership` = **70 行**（35 × spot/perp）；最终 artifact
  `sha256:05651fe0bc70726ad1a1ea05e447438509b4b9f0cf7071af7376419b5416b543.json`（70 条成员）
- **⇒ 满足 `F003` T035 的 ≥30 对前提**（tasks.md 的该前提已按批 1 实测修正为"须等批 2"）

### 本批期间的事故与处置

1. **出口被路由到美国节点 → Binance 451**：`自动选择`（URLTest）在抖动后会选中 `US1-HY2`，
   此时**所有**请求返回 451（正文 `Service unavailable from a restricted location according to
   'b. Eligibility'`，CloudFront PoP **SFO53**）。逐节点实测排除"节点故障"：同一批节点在
   NAS 侧访问 Binance 均 200；只有选中美国节点时才 451。
2. **改用德国自建节点（owner 指定）**：读本地 Clash Verge
   `profiles/de-self-built.yaml`（与 NAS `/volume1/docker/mihomo/profiles/de-self-built.yaml`
   逐字节同源）：`86.53.110.247:8443`、`vless + tls`、`flow: xtls-rprx-vision`、
   `servername: www.cloudflare.com`（Reality），与 `us-self-built` 同参仅 server 不同。
   经 mihomo API 把 `主代理` 固定为该节点后：Binance 取 1000 根 200 / 中位 **0.779 s**
   （gzip，ccxt 形态；8/8 稳定，抖动 <2%），全天未再出现 451。
3. **回填退化为 5 根/请求（本会话引入，已修）**：自动恢复包装脚本用
   `set -a; . deployment/.env` 把采集器的 `FETCH_LIMIT=5` 导出进环境，而启动器
   `scripts/f008-backfill-chunks.sh` 当时用 `FETCH="${FETCH_LIMIT:-1000}"` 读**同名**变量
   → 连续 3,402 批只推进 17,005 行（≈5 行/批，与账本增量逐行吻合），28 分钟无效。
   修复：变量改用独立命名空间 `BACKFILL_FETCH_LIMIT`，且取值移到 `source .env` **之后**；
   另核查启动器其余变量与 `.env` 无同名冲突。
4. **生产采集缺口**：出口故障期间采集同步停摆（曾出现 `symbols_ok=0 symbols_failed=6`），
   8 小时内 6 对 live pair 各累积 **84–87 分钟**缺口；用 F001 入口
   （`python -m ...historical_backfill`）窗口 `03:19Z → 12:16Z` 补齐 **3,222 行**，
   复核窗口内 **0 缺口**。
5. **与另一路的整合**：远端分支在此期间多出 `de62327`（T034：用
   `refresh_aggregates_after` 包住回填、收尾与中断都刷聚合——正是
   `test_f001_row_reconciliation` 判红的根因）。本地提交已 rebase 到其上，
   `tests/unit` 747 passed。

## 11. T023 全量导出与准入切换：**延后执行**（owner 裁决，2026-09-24）

owner 2026-09-24 指示：**全量导出与 NAS 备份实测延后**（当时执行机走手机热点网络，
不适合做大流量导出/备份），本文档先固化"下次可直接照做"的执行清单，避免现场遗失。

### 延后时的已达成状态（不需要重做）

- 回填：批 1（30/30）+ 批 2（10/10）全部 `complete`
- 门禁/准入：**35 对 ACTIVE + 5 对 QUARANTINED**；台账 70 行（35 × spot/perp）；
  artifact `sha256:05651fe0…`（70 条成员）
- **批 1 过门即可供 `F003` 使用**：`F003` T035 的 ≥30 对前提已满足（35 ≥ 30）

### 下次执行的清单（按顺序，缺一不可）

1. **追赶回填（先做，切换前）**：对**新增的 29 对**（35 已准入 − 6 基线）跑
   `--start 2026-09-10T15:52:00Z --end <切换时刻>`，把冻结窗口终点到切换时刻的洞补上。
   量级：29 对 × 约 14 天 ≈ 58 万行 ≈ 5 分钟（按 120 k 行/分）。
   （若届时距 09-10 已更久，按天数线性放大；这段必须**先补**，否则新 pair 的序列在
   冻结窗口终点处断档，实时采集从切换时刻才开始。）
2. **扩采集清单 + 重建容器**：把 `deployment/.env` 的 `SYMBOLS` 从 6 对扩到
   **35 对已准入**（隔离的 5 对不进清单，与导出清单口径一致），然后
   `docker compose up -d --force-recreate data-collector`——**必须 recreate**：
   `restart` 不重载 env（2026-09-21 代理修复时已实测踩到）。
3. **切换后核对**：采集日志应出现 `symbols_ok=35 symbols_failed=0`，且 35 对延迟一致；
   任一对失败先查出口（`BINANCE_HTTPS_PROXY` → NAS mihomo）再查节点。
4. **全量导出 + NAS 备份实测（T023 本体）**：导出可能命中 F002 的
   `guard_full_shrink`（准入过滤 + 正常生命周期收缩都会减少分区数），处置是**按 F002
   既有语义显式 `--allow-shrink` 并在容量报告记录 `shrink_confirmed`**，不得放宽守卫；
   实测磁盘占用 / 导出耗时 / 备份时长，与 6 对基线及外推值对照
   （verify: `tests/integration/test_f008_capacity_report.py`）。
5. **T026**：把上述容量与耗时（含 `hostname=qiaozhi-lt`）归档为执行机证据。
6. **T034**：回写 spec 验收证据、勾选 AC、更新 `BACKLOG.md` 与 spec frontmatter；
   并核对本文档 §9 的部署缺口（`db/migrations/005_universe_membership.sql` 必须随
   F008 并入 `main` 后部署到执行机的检出里）与 §7 的独立修复项是否已立项。

### 补充：执行机网络环境（影响归因，2026-09-24）

owner 说明该时段执行机走**手机热点**。当日多次"出口不可用"里，除 NAS mihomo 选中节点
抖动（§5）与美国节点致 Binance 451（§10）之外，**客户端侧网络切换/移动网络 NAT**
是同时存在的可疑因素（tailnet 从 direct 退化为 DERP 中继即为其典型表现，§6）。
下次做大流量操作前应先确认执行机网络稳定（tailnet `direct` 且 `ping` 抖动正常）。
